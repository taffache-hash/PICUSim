# Quickstart

```bash
pip install -r requirements.txt
python run_simulation.py --scenario scenarios/healthy_child_20kg.yaml --dt 0.5 --no-plot
```

Optional Streamlit interface:

```bash
pip install streamlit
streamlit run apps/streamlit_app.py
```
